### 🔼 Prisma

Launch prisma studio:

```sh
 npx prisma studio
```

Mitigate db

```sh
npx prisma migrate dev --name "update_description"
```

Re-generate PrismaClient

```sh
npx prisma generate
```
