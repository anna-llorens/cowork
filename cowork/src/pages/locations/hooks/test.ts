console.log("hello");

interface CompanyInfo extends Casa {
  location?: string;
  name?: string;
  id?: string;
  salary?: string;
  kununoInfo?: string;
}

type User = {
  id: any;
  name: any;
  casa: Casa;
};

console.log("hello");

interface CompanyInfo extends Casa {
  location?: string;
  name?: string;
  id?: string;
  salary?: string;
  kununoInfo?: string;
}

type User = {
  id: any;
  name: any;
  casa: Casa;
};

console.log("hello");

interface CompanyInfo extends Casa {
  location?: string;
  name?: string;
  id?: string;
  salary?: string;
  kununoInfo?: string;
}

type User = {
  id: any;
  name: any;
  casa: Casa;
};

console.log("hello");

interface CompanyInfo extends Casa {
  location?: string;
  name?: string;
  id?: string;
  salary?: string;
  kununoInfo?: string;
}

type User = {
  id: any;
  name: any;
  casa: Casa;
};

console.log("hello");

interface CompanyInfo extends Casa {
  location?: string;
  name?: string;
  id?: string;
  salary?: string;
  kununoInfo?: string;
}

type User = {
  id: any;
  name: any;
  casa: Casa;
};

console.log("hello");

interface CompanyInfo extends Casa {
  location?: string;
  name?: string;
  id?: string;
  salary?: string;
  kununoInfo?: string;
}

type User = {
  id: any;
  name: any;
  casa: Casa;
};

console.log("hello");

interface CompanyInfo extends Casa {
  location?: string;
  name?: string;
  id?: string;
  salary?: string;
  kununoInfo?: string;
}

type User = {
  id: any;
  name: any;
  casa: Casa;
};

console.log("hello");

interface CompanyInfo extends Casa {
  location?: string;
  name?: string;
  id?: string;
  salary?: string;
  kununoInfo?: string;
}

type User = {
  id: any;
  name: any;
  casa: Casa;
};

console.log("hello");

interface CompanyInfo extends Casa {
  location?: string;
  name?: string;
  id?: string;
  salary?: string;
  kununoInfo?: string;
}

type User = {
  id: any;
  name: any;
  casa: Casa;
};

console.log("hello");

interface CompanyInfo extends Casa {
  location?: string;
  name?: string;
  id?: string;
  salary?: string;
  kununoInfo?: string;
}

type User = {
  id: any;
  name: any;
  casa: Casa;
};

console.log("hello");

interface CompanyInfo extends Casa {
  location?: string;
  name?: string;
  id?: string;
  salary?: string;
  kununoInfo?: string;
}

type User = {
  id: any;
  name: any;
  casa: Casa;
};
